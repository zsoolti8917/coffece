@AGENTS.md

# Temporary Disabled Features
- **Dark mode / theme toggle**: Removed from navbar. Light mode only for now. May be re-enabled later.

# i18n
- Locales: `sk` (default, primary audience) and `en`. Configured in `src/i18n/routing.ts`.
- Language toggle lives in `src/components/layout/language-toggle.tsx` and is rendered in both the desktop navbar and the mobile sheet.
